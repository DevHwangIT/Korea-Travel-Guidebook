/**
 * Deprecated: home partner UI is now an in-flow `.partner-strip.home-partner`
 * at the bottom of the home page (no fixed rail / FAB). Kept as a no-op stub
 * so old cached HTML that still loads this file does not throw.
 */
(function () {
  /* intentionally empty */
})();
